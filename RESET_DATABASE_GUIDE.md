# 🔴 Инструкция по сбросу PostgreSQL базы данных

## ⚠️ ВНИМАНИЕ! Все данные будут удалены без возможности восстановления!

---

## 📋 Способ 1: Через скрипт (рекомендуется)

### Локально:
```bash
cd loadervodkaclientchapster2/loadervodkaclientchapster2/vodkaSite
node reset-database.js
```

### На Render:
1. Зайди в Dashboard Render
2. Открой свой сервис
3. Перейди в Shell
4. Выполни:
```bash
node reset-database.js
```

---

## 📋 Способ 2: Через API

### С помощью curl:
```bash
curl -X POST https://vodkaclient-w1i4.onrender.com/api/admin/reset-database \
  -H "Content-Type: application/json" \
  -d '{"confirm_password":"RESET_ALL_DATA_2024"}'
```

### С помощью PowerShell:
```powershell
$body = @{
    confirm_password = "RESET_ALL_DATA_2024"
} | ConvertTo-Json

Invoke-RestMethod -Uri "https://vodkaclient-w1i4.onrender.com/api/admin/reset-database" `
  -Method Post `
  -ContentType "application/json" `
  -Body $body
```

### Через админ-панель (если добавлена кнопка):
1. Открой `https://vodkaclient-w1i4.onrender.com/admin.html`
2. Найди секцию "ОПАСНАЯ ЗОНА"
3. Нажми "СБРОСИТЬ БАЗУ"
4. Введи пароль: `RESET_ALL_DATA_2024`

---

## 📋 Способ 3: Напрямую через PostgreSQL

### Если есть доступ к БД:
```sql
-- Удалить все данные
DELETE FROM users;
DELETE FROM keys;

-- Сбросить счетчики
ALTER SEQUENCE users_uid_seq RESTART WITH 1;
ALTER SEQUENCE keys_id_seq RESTART WITH 1;
```

---

## 🗑️ Что будет удалено:

- ✅ Все пользователи
- ✅ Все подписки
- ✅ Все ключи активации
- ✅ Все HWID привязки
- ✅ Счетчики UID и ID сбросятся на 1

---

## 🔐 Пароль для сброса:

```
RESET_ALL_DATA_2024
```

Если хочешь изменить пароль, отредактируй `server.js`:
```javascript
if (confirm_password !== 'ТВОЙ_НОВЫЙ_ПАРОЛЬ') {
```

---

## ✅ После сброса:

1. База данных пустая
2. Следующий пользователь получит UID = 1
3. Следующий ключ получит ID = 1
4. Все сессии станут недействительными
5. Лоадеры не смогут войти (нет пользователей)

---

## 🛠️ Проверка результата:

### Через скрипт:
```bash
node view-database.js
```

### Через API:
```bash
curl https://vodkaclient-w1i4.onrender.com/api/admin/users
```

Должен вернуть пустой массив:
```json
{
  "success": true,
  "users": []
}
```

---

## ⚡ Быстрый тест после сброса:

1. Зарегистрируй нового пользователя
2. Проверь что он получил UID = 1
3. Сгенерируй ключ
4. Проверь что он получил ID = 1

---

## 📞 Поддержка:

Если возникли проблемы, проверь:
- Переменная окружения `DATABASE_URL` настроена
- Сервер имеет доступ к PostgreSQL
- Таблицы существуют (запусти сервер для автосоздания)
