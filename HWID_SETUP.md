# 🔐 Установка HWID системы

## Что это?

HWID (Hardware ID) система привязывает аккаунт пользователя к его устройству. Один аккаунт = одно устройство.

## Что изменилось?

### Сервер (Node.js):
- ✅ Добавлены колонки `hwid` и `hwid_set_at` в таблицу users
- ✅ API `/api/launcher/check-subscription` теперь принимает и проверяет HWID
- ✅ API `/api/admin/reset-hwid` для сброса HWID пользователя
- ✅ Админ-панель показывает HWID и кнопку сброса

### Лоудер (C# WPF):
- ✅ Уже генерирует HWID и отправляет на сервер
- ✅ Обрабатывает ошибку `hwid_mismatch`
- ✅ Показывает понятное сообщение при несовпадении HWID

## Установка (для существующей базы данных):

### Шаг 1: Миграция базы данных

Запусти файл `4-migrate-hwid.bat` или выполни команду:

```bash
node migrate-hwid.js
```

Это добавит колонки `hwid` и `hwid_set_at` в таблицу users.

### Шаг 2: Перезапусти сервер

```bash
node server.js
```

Или используй `1-start-server.bat`

### Шаг 3: Готово!

Теперь:
- При первом входе пользователя HWID сохраняется
- При повторном входе HWID проверяется
- Если HWID не совпадает - доступ запрещен
- Админ может сбросить HWID через админ-панель

## Как это работает?

### Первый вход пользователя:
1. Пользователь вводит логин и пароль в лоудере
2. Лоудер генерирует HWID устройства (CPU + материнка + BIOS + диск)
3. Лоудер отправляет логин, пароль и HWID на сервер
4. Сервер проверяет логин/пароль
5. Если HWID не установлен - сервер сохраняет его
6. Пользователь получает доступ

### Повторный вход:
1. Пользователь вводит логин и пароль
2. Лоудер генерирует HWID
3. Сервер сравнивает HWID с сохраненным
4. Если совпадает - доступ разрешен
5. Если НЕ совпадает - доступ запрещен

### Смена устройства:
1. Пользователь пишет в поддержку
2. Админ заходит в админ-панель
3. Админ нажимает "Сбросить HWID" для пользователя
4. Пользователь может войти с нового устройства
5. Новый HWID сохраняется

## Админ-панель

### Просмотр HWID:
- Открой `admin.html`
- Войди (логин: ServerVodki, пароль: irairairA1)
- В таблице пользователей видно HWID каждого

### Сброс HWID:
- Нажми кнопку "Сбросить HWID" рядом с пользователем
- Подтверди действие
- HWID будет удален из базы
- Пользователь сможет войти с нового устройства

## Безопасность

### Что защищает HWID:
- ✅ Один аккаунт нельзя использовать на нескольких ПК
- ✅ Нельзя передать аккаунт другу
- ✅ Нельзя продать аккаунт

### Что НЕ защищает:
- ❌ Виртуализацию (можно обойти через VM)
- ❌ Продвинутых читеров (могут подменить HWID)

### Рекомендации:
- Используй HTTPS для всех запросов
- Не показывай полный HWID пользователю
- Логируй все попытки входа с неправильным HWID
- Добавь лимит попыток входа

## Тестирование

### Тест 1: Первый вход
1. Создай нового пользователя на сайте
2. Войди через лоудер
3. Проверь в админ-панели - HWID должен быть установлен

### Тест 2: Повторный вход
1. Закрой лоудер
2. Открой снова и войди
3. Должно работать без проблем

### Тест 3: Несовпадение HWID
1. В админ-панели сбрось HWID пользователя
2. Измени код в HwidService.cs (добавь "TEST" к HWID)
3. Попробуй войти
4. Должна быть ошибка "Устройство не совпадает"

### Тест 4: Сброс HWID
1. Сбрось HWID через админ-панель
2. Войди снова
3. Новый HWID должен сохраниться

## Troubleshooting

### Ошибка "HWID не передан"
- Проверь что лоудер отправляет HWID в запросе
- Проверь что HwidService.GetHwid() работает

### Ошибка "duplicate column"
- Колонки уже добавлены, миграция не нужна
- Просто перезапусти сервер

### HWID всегда разный
- Проверь что HwidService кеширует HWID
- Проверь что железо не меняется между запусками

### Админ-панель не показывает HWID
- Обнови страницу (Ctrl+F5)
- Проверь что миграция выполнена
- Проверь что сервер обновлен

## API Endpoints

### POST /api/launcher/check-subscription
Проверка логина, пароля и HWID

**Запрос:**
```json
{
  "username": "test",
  "password": "123456",
  "hwid": "A1B2C3D4..."
}
```

**Ответ (успех):**
```json
{
  "success": true,
  "has_subscription": true,
  "user": {...},
  "subscription": {...}
}
```

**Ответ (HWID не совпадает):**
```json
{
  "success": false,
  "message": "Устройство не совпадает...",
  "has_subscription": false,
  "hwid_mismatch": true
}
```

### POST /api/admin/reset-hwid
Сброс HWID пользователя (только для админов)

**Запрос:**
```json
{
  "uid": 1
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "HWID сброшен"
}
```

## Поддержка

Telegram: @SmertnixVteste
